package mypage;

import java.sql.SQLException;
import java.util.ArrayList;

import model.BuyProductVO;
import model.MyPageDAO;


public class TestCaseMyBuyProductList {
	public static void main(String[] args) {
		/*
		 * String status="확인중"; String id="java"; ArrayList<BuyProductVO> list = new
		 * ArrayList<>(); try { list
		 * =MyPageDAO.getInstance().myBuyProductList(status,id);
		 * System.out.println(list.toString()); } catch (SQLException e) {
		 * e.printStackTrace(); }
		 */
		//dao 바꿔서 주석처리
	}
}
